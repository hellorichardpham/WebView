<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:nestedScrollingEnabled="false">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="San Jose Mercury"
        android:id="@+id/MercuryButton"
        android:layout_marginTop="54dp"
        android:layout_centerHorizontal="true"
        android:onClick="onWebClick" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SF Gate"
        android:id="@+id/SFGateButton"
        android:onClick="onWebClick"
        android:layout_marginTop="83dp"
        android:layout_below="@+id/MercuryButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Santa Cruz Sentinel"
        android:id="@+id/SentinelButton"
        android:onClick="onWebClick"
        android:layout_below="@+id/SFGateButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="106dp" />

</RelativeLayout>
